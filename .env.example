# Base URL of lift-tracker-backend (sibling repo, one level up). Maps to
# runtimeConfig.public.apiBaseUrl (nuxt.config.ts) via Nuxt's NUXT_PUBLIC_* env convention.
NUXT_PUBLIC_API_BASE_URL=http://localhost:6600
